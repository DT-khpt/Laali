name: mcp
description: "Maternal health voice assistant in Kannada"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  flutter_tts: ^4.2.3
  speech_to_text: ^7.0.1
  http: ^1.5.0
  path_provider: ^2.1.1
  audioplayers: ^6.5.1
  record: ^4.4.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

